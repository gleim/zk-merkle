import { MerkleRootStorageContract } from './MerkleRootStorageContract';

describe('MerkleRootStorageContract.js', () => {
  describe('MerkleRootStorageContract()', () => {
    it.todo('should be correct');
  });
});
